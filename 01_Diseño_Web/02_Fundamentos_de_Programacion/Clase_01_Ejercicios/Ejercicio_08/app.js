// Solicitar al usuario la medida de la base y de la altura de un rectángulo y devolver el área.

const base = prompt ("¿Dime la medida de la base?");

const altura = prompt ("¿Dime la medida de la base?");

const area = parseFloat(base) * parseFloat(altura);

alert ("El área del rectángulo es " + area);