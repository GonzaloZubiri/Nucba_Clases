// Solicitar al usuario que ingrese una palabra y mostrar con qué letra termina.

const text = prompt("Ingresa una palabra");

// document.write(text.charAt(text.length - 1));

console.log(text.charAt(text.length - 1));